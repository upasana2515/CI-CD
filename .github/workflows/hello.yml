name: My first workflow
on: push
jobs:
  say-hello:
    runs-on: ubuntu-latest
  say-byee:
    runs-on: ubuntu-latest
    steps:
      - name:  Print message
        run: echo "Hello, github Actions!"
      - name:  Print message
        run: echo "Bye, github Actions!"